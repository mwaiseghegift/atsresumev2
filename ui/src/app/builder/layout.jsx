export const metadata = {
  title: "Resume Builder",
  description:
    "Build your ATS-optimized resume with AI. Customize for any job description using Google Gemini AI and download a professional PDF — free.",
  alternates: { canonical: "/builder" },
};

export default function BuilderLayout({ children }) {
  return children;
}
