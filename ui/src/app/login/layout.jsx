export const metadata = {
  title: "Sign In",
  description:
    "Sign in to your ATS Resume V2 account to access your saved resumes and AI customizations.",
  robots: { index: false, follow: false },
};

export default function LoginLayout({ children }) {
  return children;
}
