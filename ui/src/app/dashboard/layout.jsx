export const metadata = {
  title: "Dashboard",
  description: "Manage your saved resumes and AI-customized resume versions.",
  robots: { index: false, follow: false },
};

export default function DashboardLayout({ children }) {
  return children;
}
